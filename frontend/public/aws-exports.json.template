{
  "Auth": {
    "Cognito": {
      "userPoolId": "{{USER_POOL_ID}}",
      "userPoolClientId": "{{USER_POOL_CLIENT_ID}}",
      "loginWith": {
        "oauth": {
          "domain": "{{COGNITO_DOMAIN}}",
          "scopes": ["openid", "email", "profile"],
          "redirectSignIn": ["{{FRONTEND_URL}}"],
          "redirectSignOut": ["{{FRONTEND_URL}}"],
          "responseType": "code"
        },
        "username": true,
        "email": false,
        "phone": false
      }
    }
  }
}
